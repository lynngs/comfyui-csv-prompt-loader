[project]
name = "comfyui-csv-prompt-loader"
description = "A ComfyUI node that loads prompts one row at a time from a CSV file (column 1 = output filename, column 2 = prompt), with auto-incrementing or fixed-index selection."
version = "1.1.0"
license = { text = "MIT" }
dependencies = []

[project.urls]
Repository = "https://github.com/lynngs/comfyui-csv-prompt-loader"

[tool.comfy]
PublisherId = "lynngs"
DisplayName = "CSV Prompt Loader"
Repository = "https://github.com/lynngs/comfyui-csv-prompt-loader"
